const continueShop = (id) => {
  const htmlStr = `
    <a href="/" class="${id}__continueshop">
        <span>Fortsæt med at handle</span>
    </a>
    `;
  return htmlStr;
};

export default continueShop;
