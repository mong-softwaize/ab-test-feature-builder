const formWrapper = (id) => {
  const htmlStr = `<div class="${id}__hbsptcontainer">
    <h2>SIGN UP FOR OUR E-BLASTS</h2>
        <div class="form-wrapper container-slim"></div>
    </div>`;
  return htmlStr.trim();
};

export default formWrapper;
